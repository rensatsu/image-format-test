# image-format-test

Simple website to test available modern image formats in browsers.

## Build and deploy

Install `netlify-cli`.

Build app:

```bash
npm run build
```

Deploy app:

```bash
npm run deploy
```
